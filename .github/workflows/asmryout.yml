name: 🧠 Post EMDR Short to YouTube

on:
  workflow_dispatch:
  schedule:
    - cron: "0 19 * * 1,3,5"  # Mon Wed Fri 2PM EST

jobs:
  record:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y xvfb chromium-browser libnss3 libgbm1 libasound2 ffmpeg pulseaudio
          pip install selenium webdriver-manager google-auth google-auth-oauthlib google-api-python-client
          echo "✅ Done"

      - name: Set timestamp
        run: echo "TS=$(date +%Y%m%d_%H%M%S)" >> $GITHUB_ENV

      - name: Verify asmry.html
        run: ls -la asmry.html && echo "✅ Found"

      - name: Start display and audio
        run: |
          Xvfb :99 -screen 0 1080x1920x24 -ac &
          sleep 2
          pulseaudio --start --exit-idle-time=-1 &
          sleep 2
          echo "DISPLAY=:99" >> $GITHUB_ENV
          echo "✅ Display ready"

      - name: Record EMDR Short
        run: |
          python3 << 'PYEOF'
          import os, time, base64, sys
          from selenium import webdriver
          from selenium.webdriver.chrome.options import Options
          from selenium.webdriver.chrome.service import Service
          from webdriver_manager.chrome import ChromeDriverManager

          html_path = os.path.abspath("asmry.html")
          if not os.path.exists(html_path):
            print(f"❌ asmry.html not found at {html_path}")
            sys.exit(1)
          print(f"✅ Found {html_path}")

          webm_file = f"/tmp/emdr_{os.environ.get('TS','test')}.webm"

          opts = Options()
          opts.add_argument("--no-sandbox")
          opts.add_argument("--disable-dev-shm-usage")
          opts.add_argument("--disable-gpu")
          opts.add_argument("--window-size=1080,1920")
          opts.add_argument("--window-position=0,0")
          opts.add_argument("--force-device-scale-factor=1")
          opts.add_argument("--autoplay-policy=no-user-gesture-required")
          opts.add_argument("--disable-background-timer-throttling")
          opts.add_argument("--disable-renderer-backgrounding")
          opts.add_argument("--disable-backgrounding-occluded-windows")
          opts.add_argument("--no-first-run")
          opts.add_argument("--disable-infobars")
          opts.add_argument("--disable-notifications")
          opts.add_argument("--use-fake-ui-for-media-stream")
          opts.add_argument("--use-fake-device-for-media-stream")
          opts.add_argument("--allow-file-access-from-files")
          opts.add_experimental_option("excludeSwitches", ["enable-automation"])
          opts.add_experimental_option("useAutomationExtension", False)
          opts.add_experimental_option("mobileEmulation", {
            "deviceMetrics": {"width": 1080, "height": 1920, "pixelRatio": 1.0},
            "userAgent": "Mozilla/5.0 (Linux; Android 12; Pixel 6) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/112.0.0.0 Mobile Safari/537.36"
          })

          print("🚀 Starting Chrome 1080x1920...")
          driver = webdriver.Chrome(service=Service(ChromeDriverManager().install()), options=opts)
          driver.set_window_size(1080, 1920)
          driver.set_script_timeout(90)

          print(f"📄 Loading {html_path}")
          driver.get(f"file://{html_path}")
          time.sleep(3)
          driver.execute_script("if(typeof AC!=='undefined'&&AC)AC.resume().catch(()=>{});")
          time.sleep(1)

          driver.execute_script("window.startRecording(30);")
          print("🎬 Recording — waiting ~22s...")

          for i in range(60):
            done = driver.execute_script("return window._recordingDone;")
            if done:
              print(f"✅ Done at {i*0.5:.0f}s")
              break
            time.sleep(0.5)
          else:
            print("⏰ Timeout — forcing stop")
            driver.execute_script("window.stopRecording();")
            time.sleep(2)
            driver.execute_script("window.forceFinalize();")
            time.sleep(2)

          if not driver.execute_script("return window._recordingDone;"):
            driver.quit()
            print("❌ Recording failed")
            sys.exit(1)

          print("📦 Extracting...")
          b64 = driver.execute_async_script("""
            var done = arguments[0];
            window.getRecordingBase64().then(done).catch(function(e){ console.error(e); done(null); });
          """)
          driver.quit()

          if not b64:
            print("❌ No video data")
            sys.exit(1)

          with open(webm_file, "wb") as f:
            f.write(base64.b64decode(b64))
          size = os.path.getsize(webm_file)
          print(f"✅ Webm: {size/1024/1024:.1f} MB")
          if size < 10000:
            print("❌ Too small")
            sys.exit(1)

          mp4_file = webm_file.replace(".webm", ".mp4")
          import subprocess
          result = subprocess.run([
            "ffmpeg", "-y", "-i", webm_file,
            "-c:v", "libx264", "-preset", "slow", "-crf", "16",
            "-pix_fmt", "yuv420p",
            "-vf", "scale=1080:1920:flags=lanczos",
            "-c:a", "aac", "-b:a", "192k", "-ar", "44100",
            "-movflags", "+faststart",
            mp4_file
          ], capture_output=True, text=True)

          if result.returncode != 0:
            print(f"❌ ffmpeg error:\n{result.stderr[-2000:]}")
            sys.exit(1)

          os.remove(webm_file)
          print(f"✅ mp4: {os.path.getsize(mp4_file)/1024/1024:.1f} MB")

          # Save path for next step
          with open("/tmp/mp4_path.txt", "w") as f:
            f.write(mp4_file)
          PYEOF
        env:
          DISPLAY: ":99"
          TS: ${{ env.TS }}

      - name: Upload to YouTube as Short
        run: |
          python3 << 'PYEOF'
          import os, random, sys
          from googleapiclient.discovery import build
          from googleapiclient.http import MediaFileUpload
          from google.oauth2.credentials import Credentials

          with open("/tmp/mp4_path.txt") as f:
            mp4_file = f.read().strip()

          if not os.path.exists(mp4_file):
            print("❌ mp4 not found")
            sys.exit(1)

          videos = [
            {
              "title": "Bilateral Stimulation | Alpha Waves for Deep Relaxation 🧠 #Shorts",
              "description": """⚠️ DISCLAIMER: For relaxation and entertainment only. Do not use if you have epilepsy, prone to seizures, or sensitive to flashing lights. Not medical advice. Stop if discomfort. Do not drive or operate machinery.

        🎧 USE HEADPHONES for left-right audio effect.

        🧠 What is this?
        Bilateral stimulation + brainwave entrainment. Sound alternates left-right while you follow the moving visual. Alpha waves (8-12 Hz) promote deep relaxation and calm the nervous system.

        Benefits:
        ✓ Reduces anxiety and stress
        ✓ Calms the nervous system
        ✓ Promotes emotional regulation
        ✓ Helps with relaxation

        How to use:
        1. Put on headphones 🎧
        2. Follow the moving ball with your eyes
        3. Breathe slowly and relax

        #bilateralstimulation #alphawaves #brainflossing #binauralbeats #brainwaveentrainment #relaxation #anxietyrelief #stressrelief #mentalhealth #nervoussystem #shorts"""
            },
            {
              "title": "Brain Flossing | Fast Bilateral Beats for Focus 🎧 #Shorts",
              "description": """⚠️ DISCLAIMER: For relaxation and entertainment only. Do not use if you have epilepsy, prone to seizures, or sensitive to flashing lights. Not medical advice. Stop if discomfort. Do not drive or operate machinery.

        🎧 USE HEADPHONES for left-right audio effect.

        🧠 What is this?
        Bilateral stimulation + brainwave entrainment. Beta waves (13-30 Hz) promote focus, concentration, and alertness.

        Benefits:
        ✓ Improves focus and concentration
        ✓ Enhances mental clarity
        ✓ Reduces brain fog
        ✓ Sharpens alertness

        How to use:
        1. Put on headphones 🎧
        2. Follow the moving ball with your eyes
        3. Stay focused on the movement

        #bilateralstimulation #betawaves #brainflossing #binauralbeats #brainwaveentrainment #focus #concentration #mentalclarity #productivity #shorts"""
            },
            {
              "title": "Bilateral Beats + Fast Visual Sync | Theta Waves for Anxiety Relief #Shorts",
              "description": """⚠️ DISCLAIMER: For relaxation and entertainment only. Do not use if you have epilepsy, prone to seizures, or sensitive to flashing lights. Not medical advice. Stop if discomfort. Do not drive or operate machinery.

        🎧 USE HEADPHONES for left-right audio effect.

        🧠 What is this?
        Bilateral stimulation + brainwave entrainment. Theta waves (4-8 Hz) promote deep relaxation, meditation, and anxiety relief.

        Benefits:
        ✓ Reduces anxiety and stress
        ✓ Promotes deep relaxation
        ✓ Supports meditation
        ✓ Enhances emotional regulation

        How to use:
        1. Put on headphones 🎧
        2. Follow the moving ball with your eyes
        3. Let thoughts pass without engaging

        #bilateralstimulation #thetawaves #brainflossing #binauralbeats #brainwaveentrainment #anxietyrelief #stressrelief #meditation #mentalhealth #shorts"""
            },
            {
              "title": "Brain Massage in 60s | Bilateral Stimulation + Delta Waves for Sleep #Shorts",
              "description": """⚠️ DISCLAIMER: For relaxation and entertainment only. Do not use if you have epilepsy, prone to seizures, or sensitive to flashing lights. Not medical advice. Stop if discomfort. Do not drive or operate machinery.

        🎧 USE HEADPHONES for left-right audio effect.

        🧠 What is this?
        Bilateral stimulation + brainwave entrainment. Delta waves (0.5-4 Hz) promote deep sleep and healing rest.

        Benefits:
        ✓ Promotes deep sleep
        ✓ Enhances healing and recovery
        ✓ Reduces insomnia
        ✓ Supports nervous system regulation

        How to use:
        1. Put on headphones 🎧
        2. Follow the moving ball with your eyes
        3. Relax and let go

        #bilateralstimulation #deltawaves #brainflossing #binauralbeats #brainwaveentrainment #sleep #insomnia #deeprelaxation #healing #shorts"""
            },
            {
              "title": "Sync Your Brain Fast | Bilateral Stimulation + Gamma Waves for Focus #Shorts",
              "description": """⚠️ DISCLAIMER: For relaxation and entertainment only. Do not use if you have epilepsy, prone to seizures, or sensitive to flashing lights. Not medical advice. Stop if discomfort. Do not drive or operate machinery.

        🎧 USE HEADPHONES for left-right audio effect.

        🧠 What is this?
        Bilateral stimulation + brainwave entrainment. Gamma waves (30-100 Hz) promote peak performance, cognitive enhancement, and focus.

        Benefits:
        ✓ Enhances cognitive performance
        ✓ Improves focus and concentration
        ✓ Boosts memory and learning
        ✓ Promotes peak mental state

        How to use:
        1. Put on headphones 🎧
        2. Follow the moving ball with your eyes
        3. Stay engaged and alert

        #bilateralstimulation #gammawaves #brainflossing #binauralbeats #brainwaveentrainment #focus #cognitiveenhancement #peakperformance #mentalhealth #shorts"""
            }
          ]

          video = random.choice(videos)
          print(f"📹 Title: {video['title']}")

          creds = Credentials(
            token=os.environ.get("YT_ACCESS_TOKEN"),
            refresh_token=os.environ.get("YT_REFRESH_TOKEN"),
            token_uri="https://oauth2.googleapis.com/token",
            client_id=os.environ.get("YT_CLIENT_ID"),
            client_secret=os.environ.get("YT_CLIENT_SECRET"),
          )
          youtube = build("youtube", "v3", credentials=creds)

          request = youtube.videos().insert(
            part="snippet,status",
            body={
              "snippet": {
                "title": video["title"],
                "description": video["description"],
                "tags": [
                  "bilateral stimulation","bilateralstimulation","brain flossing","brainflossing",
                  "binaural beats","binauralbeats","brainwave entrainment","brainwaveentrainment",
                  "alpha waves","alphawaves","beta waves","betawaves","gamma waves","gammawaves",
                  "theta waves","thetawaves","delta waves","deltawaves",
                  "anxiety relief","stress relief","relaxation","focus","concentration",
                  "sleep","meditation","mental health","nervous system","nervoussystem",
                  "emdr","self therapy","shorts"
                ],
                "categoryId": "26",
                "defaultLanguage": "en"
              },
              "status": {
                "privacyStatus": "public",
                "madeForKids": False,
                "selfDeclaredMadeForKids": False
              }
            },
            media_body=MediaFileUpload(mp4_file, mimetype="video/mp4", resumable=True)
          )

          response = None
          while response is None:
            status, response = request.next_chunk()
            if status:
              print(f"⬆️  {int(status.progress()*100)}%")

          video_id = response.get("id","")
          print(f"✅ Uploaded! https://youtube.com/shorts/{video_id}")
          os.remove(mp4_file)
          PYEOF
        env:
          YT_ACCESS_TOKEN: ${{ secrets.YT_ACCESS_TOKEN }}
          YT_REFRESH_TOKEN: ${{ secrets.YT_REFRESH_TOKEN }}
          YT_CLIENT_ID: ${{ secrets.YT_CLIENT_ID }}
          YT_CLIENT_SECRET: ${{ secrets.YT_CLIENT_SECRET }}